public enum TokenType {
	ATTR,
	INT_TYPE, CHAR_TYPE, BOOL_TYPE, 
	IF_DECL, ELSE_DECL, 
	WHILE_DECL,
	NUMBER, CHAR, BOOL, STRING,
	ID, PRINT,
	EQ, NE, LT, GT, LE, GE,
	COMMA, SEMICOLLON,
	PLUS, MINUS, MULT, DIV,
	L_PAR, R_PAR,L_BRACE, R_BRACE;
}
